/**
 *
 * 异步发送请求,获取数据
 * Vue-Resource------Vue1.x版本中使用
 * axios-----Vue2.x
 * 所在的方法的名字的前面 async
 * await axios.get('url')
 * await axios.get('url',{
 *  params:{
 *    q:名字
 * }
 * })
 *
 * response =await axios.get()
 *
 * const result=response.data
 *
 * a标签href="#"---------跳转
 * 路由:锚点值改变,在界面中的指定位置刷新DOM的(重新渲染DOM)
 * 路由器: 应该包括多个路由,每个路由都是由路由组件组成
 * pages中创建.vue文件,在router目录中的index.js中配置路由,每个.vue文件就是一个路由组件
 * 所有的路由组件配置后,最终在Vue的实例中进行挂载,注册,此时的router叫路由器
 *
 * 配置路由:
 * mode:'history'
 * routes:[
 *  {
 *  path:'地址',
 *  name:''
 *  component:About
 * },
 * {
 *  path:'地址',
 * redirect:'重新定向的地址'
 * }
 *
 * ]
 *
 *
 *
 *
 *
 *
 *
 *
 */